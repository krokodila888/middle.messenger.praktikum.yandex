import './change-chat-avatar.scss';
import ChangeChatAvatarRaw from './change-chat-avatar.hbs?raw';
import Block from '../../tools/Block';
/*import { InputButton, ProfileFileInput, ChangeChatAvatarButton } from '..';

interface Props {
  [key: string]: string;
}

export class ChangeChatAvatar extends Block {
  constructor(props: Props) {
    super({
      ...props,
        inputFile: new ProfileFileInput({
          className: props.className,
          name: props.name,
          value: props.value,
        }),
        buttonDiv: new ChangeChatAvatarButton({

        })

      })
    }
  render() {
    return ChangeChatAvatarRaw;
  }
}
*/